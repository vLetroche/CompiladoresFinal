#include <stdio.h>
#include <stdlib.h>

int main() {
    int fatorial = 0;
    int parametro = 0;

    scanf("%d", &parametro);
    fatorial = 1;
    fatorial = parametro;
    if ((parametro==0)) {
    }
    else {
    }

    return 0;
}
