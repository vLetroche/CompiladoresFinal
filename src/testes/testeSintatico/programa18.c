#include <stdio.h>
#include <stdlib.h>

int main() {
    int fatorial = 0;
    int parametro = 0;

    scanf("%d", &parametro);
    fatorial = 1;
    fatorial = parametro;
    if ((parametro==0)) {
    }
    else {
    }
    fatorial = (fatorial*(parametro-1));
    parametro = parametro-1;
    while ((parametro>1)) {
    }
    printf("%d\n", fatorial);
    printf("Oi");

    return 0;
}
